services:
  dev:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      args:
        UID: "${DOCKER_UID:-1000}"
        GID: "${DOCKER_GID:-1000}"
    image: robotics-turtlebot3:dev
    container_name: robotics-turtlebot3
    hostname: robotics-turtlebot3
    network_mode: host
    ipc: host
    environment:
      - ROS_DOMAIN_ID=${ROS_DOMAIN_ID:-31}
      - RMW_IMPLEMENTATION=rmw_fastrtps_cpp
      - DISPLAY=${DISPLAY}
      - QT_X11_NO_MITSHM=1
    volumes:
      - ../src:/ws/src
      - ../config:/ws/config
      - ../launch:/ws/launch
      - ../data:/ws/data
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - ws_build:/ws/build
      - ws_install:/ws/install
    stdin_open: true
    tty: true

volumes:
  ws_build:
  ws_install:
